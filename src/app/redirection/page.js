import { redirect } from "next/navigation";

const Redirection = () => {
    // Method to redirect
    // redirect("/");

    // for method 2 we have to do changes in next.config.mjs pls check the next.config.mjs file
    return (
        <>
            <h1>Redirection Page</h1>
        </>
    )
}

export default Redirection;